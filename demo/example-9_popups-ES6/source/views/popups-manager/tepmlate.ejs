<% var Factory = require('./factory');
console.log(this.props.toJSON(), Factory);
for (var viewID in this.props.toJSON()) { %>

    <::Factory key="<%= viewID %>" popup-content="<%= this.props.get(viewID) %>"/>

<% } %>